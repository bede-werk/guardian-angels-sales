{
  "name": "guardian-angels-sales",
  "version": "1.0.0",
  "private": true,
  "description": "Sales visit scheduler for Guardian Angels Homecare (Lincoln, NE)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm --prefix server install && npm --prefix client install && npm --prefix client run build",
    "heroku-postbuild": "npm run build",
    "start": "npm --prefix server run start",
    "migrate": "npm --prefix server run migrate",
    "import": "npm --prefix server run import",
    "seed": "npm --prefix server run seed",
    "dev": "./dev.sh"
  }
}
